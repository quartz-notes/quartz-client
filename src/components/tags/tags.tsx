// import useNoteStore from '@/app/stores/notes.store';

import { Badge } from '@/shared/ui/badge';

function Tags() {
	// const currentNote = useNoteStore(state => state.currentNote);
	// const noteTags = useNoteStore(state =>
	// 	state.notes.find(note => note.id === currentNote)
	// );
	return (
		<div className='p-4'>
			<Badge>ffff</Badge>
		</div>
	);
}

export default Tags;
